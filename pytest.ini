[pytest]
# Global flags: verbose output and skip technical noise
addopts = -v -s --tb=short
markers =
    smoke: Critical infrastructure checks
    security: Security audit and hardening
    network: Performance and latency tests

# Hide technical warnings from libraries
filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning